version: v0.0.1
activate: true
columns:
  id:
    goType: int
    tags:
      - primaryKey
    comment: 등록번호
  password:
    goType: string
    dbType: varchar(200)
    comment: 비밀번호
binds:
  wallets:
    model: wallets
    local: id
    foreign: userId
    type: list
indexes:
  password:
    - password
hooks:
  timeAt:
    - createdAt
    - updatedAt
    - deletedAt